<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Jaringan & sinkronisasi ──────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- WorkManager (M8): antrean penjualan tetap terkirim walau aplikasi tertutup. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Dialog pengecualian optimasi baterai. Tanpa pengecualian ini, MIUI /
         ColorOS / FunTouch membunuh isolate WorkManager dan sinkronisasi latar
         diam tanpa pesan error apa pun ([09 §6.4]). -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- ── Bluetooth Android 12+ (API 31) ───────────────────────────────────
         WAJIB diminta saat runtime lewat PrinterRegistry.ensureBluetoothPermissions().
         Tanpa itu, adapter gagal dengan SecurityException yang pesannya sama
         sekali tidak menunjuk ke izin ([09 §4.6]).

         `neverForLocation` menyatakan aplikasi tidak menurunkan posisi pengguna
         dari hasil pemindaian — tanpa bendera ini, Android menuntut izin lokasi
         penuh hanya untuk mencari printer. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>

    <!-- ── Bluetooth Android ≤ 11 ───────────────────────────────────────────
         Desain lama menganggap pemindaian BLE dapat menurunkan posisi
         pengguna, sehingga izin lokasi menjadi syarat. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"/>

    <!-- Perangkat tanpa Bluetooth tetap dapat memakai printer USB, LAN, atau
         printer internal handheld — jangan menyaringnya dari Play Store. -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false"/>

    <application
        android:label="POS Godinov"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"

        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- 🔴 allowBackup="false" BUKAN opsi.
             Basis data lokal memuat `pin_hash` bcrypt SELURUH kasir outlet
             ([03 §2.2]). Android Auto Backup akan menyalinnya ke Google Drive
             milik akun perangkat, memindahkan seluruh permukaan serangan ke
             luar kendali outlet ([09 §4.4]). -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"

            android:lockTaskMode="if_whitelisted">
            <!-- `if_whitelisted` mengizinkan Lock Task Mode hanya bila paket ini
                 ada di daftar `setLockTaskPackages` — yang hanya dapat diisi
                 oleh Device Owner ([09 §4.4]). -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Menjadikan aplikasi sebagai Home launcher.
                 Tanpa ini, tombol Home pada perangkat NON-Device-Owner tetap
                 memindahkan pelanggan keluar dari Kiosk. Pada perangkat
                 Device Owner, Home sudah dimatikan `setLockTaskFeatures`
                 sehingga filter ini menjadi lapis kedua. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Syarat menjadi Device Owner. Tanpa receiver ini,
             `adb shell dpm set-device-owner` menolak tanpa penjelasan. -->
        <receiver
            android:name=".kiosk.GodinovDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Service AIDL printer internal Sunmi/iMin. Tanpa deklarasi ini,
             Android 11+ menyembunyikan paketnya dari bindService(). -->
        <package android:name="woyou.aidlservice.jiuiv5"/>
    </queries>
</manifest>
